# API implementation scope

The platform specification is maintained in the parent workspace at `../SPEC.md`.
This repository implements the API service described there. Phase 0 provides
only repository setup and the health endpoint; later phases must be completed
and tested one at a time.
